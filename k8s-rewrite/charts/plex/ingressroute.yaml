# Traefik IngressRoute for Plex
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: plex-ingress
  namespace: plex
spec:
  entryPoints:
  - web
  routes:
  - match: Host(`plex.branconet.local`)
    services:
    - name: plex
      port: 32400
