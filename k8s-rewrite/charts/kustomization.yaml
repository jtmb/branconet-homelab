---
# Root Kustomization — synced by Flux Kustomizations test-stack & media-stack
# Each stack watches its own subdirectory under ./k8s-rewrite/charts/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./test-stack
  - ./media-stack


