# FlareSolverr Service (internal — no ingress)
apiVersion: v1
kind: Service
metadata:
  name: flaresolverr
  namespace: plex
spec:
  type: ClusterIP
  ports:
  - port: 8191
    targetPort: 8191
    name: http
  selector:
    app: flaresolverr
