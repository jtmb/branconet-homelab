# Plex Ingress (standard Kubernetes Ingress, not Traefik CRD)
# Traefik picks this up via --providers.kubernetesingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plex-ingress
  namespace: plex
spec:
  rules:
  - host: plex.branconet.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: plex
            port:
              number: 32400
