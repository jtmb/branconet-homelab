# nginx-hello Service
apiVersion: v1
kind: Service
metadata:
  name: nginx-hello
  namespace: nginx-hello
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    app: nginx-hello
