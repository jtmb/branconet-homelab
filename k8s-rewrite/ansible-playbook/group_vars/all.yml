# Kubernetes Cluster Configuration Variables
# Auto-generated from Botrus K8s DB
# Last synced: 2026-06-16T04:18:46.604Z
# WARNING: Manual edits to this file will be overwritten on next deploy/sync.

---

# =============================================================================
# ANSIBLE
# =============================================================================
ansible_become_password: "{{ lookup('botrus_secret', 'ansible_become_password') }}"
ansible_ssh_private_key_file: "{{ lookup('botrus_secret', 'ansible_ssh_private_key_file') }}"

# =============================================================================
# DNS
# =============================================================================
dns_servers: "{{ lookup('botrus_secret', 'dns_servers') }}"

# =============================================================================
# GENERAL
# =============================================================================
github_token: "{{ lookup('botrus_secret', 'github_token') }}"

# =============================================================================
# HELM
# =============================================================================
helm_version: "{{ lookup('botrus_secret', 'helm_version') }}"

# =============================================================================
# KUBERNETES
# =============================================================================
calico_version: "{{ lookup('botrus_secret', 'calico_version') }}"
cluster_name: "{{ lookup('botrus_secret', 'cluster_name') }}"
coredns_replicas: "{{ lookup('botrus_secret', 'coredns_replicas') }}"
crictl_version: "{{ lookup('botrus_secret', 'crictl_version') }}"
k8s_version: "{{ lookup('botrus_secret', 'k8s_version') }}"
kubeconfig_path: "{{ lookup('botrus_secret', 'kubeconfig_path') }}"
node_u1_hostname: "{{ lookup('botrus_secret', 'node_u1_hostname') }}"
node_u1_ip: "{{ lookup('botrus_secret', 'node_u1_ip') }}"
node_u1_name: "{{ lookup('botrus_secret', 'node_u1_name') }}"
node_u1_role: "{{ lookup('botrus_secret', 'node_u1_role') }}"
node_u2_hostname: "{{ lookup('botrus_secret', 'node_u2_hostname') }}"
node_u2_ip: "{{ lookup('botrus_secret', 'node_u2_ip') }}"
node_u2_name: "{{ lookup('botrus_secret', 'node_u2_name') }}"
node_u2_role: "{{ lookup('botrus_secret', 'node_u2_role') }}"
node_u3_hostname: "{{ lookup('botrus_secret', 'node_u3_hostname') }}"
node_u3_ip: "{{ lookup('botrus_secret', 'node_u3_ip') }}"
node_u3_name: "{{ lookup('botrus_secret', 'node_u3_name') }}"
node_u3_role: "{{ lookup('botrus_secret', 'node_u3_role') }}"

# =============================================================================
# LONGHORN
# =============================================================================
longhorn_default_storage_class: "{{ lookup('botrus_secret', 'longhorn_default_storage_class') }}"
longhorn_replicas: "{{ lookup('botrus_secret', 'longhorn_replicas') }}"
longhorn_version: "{{ lookup('botrus_secret', 'longhorn_version') }}"

# =============================================================================
# NETWORKING
# =============================================================================
cluster_cidr: "{{ lookup('botrus_secret', 'cluster_cidr') }}"
cluster_domain: "{{ lookup('botrus_secret', 'cluster_domain') }}"
pod_network_cidr: "{{ lookup('botrus_secret', 'pod_network_cidr') }}"
service_cidr: "{{ lookup('botrus_secret', 'service_cidr') }}"

# =============================================================================
# NFS
# =============================================================================
nfs_enabled: "{{ lookup('botrus_secret', 'nfs_enabled') }}"
nfs_provisioner_version: "{{ lookup('botrus_secret', 'nfs_provisioner_version') }}"
nfs_server_path: "{{ lookup('botrus_secret', 'nfs_server_path') }}"
nfs_storage_class: "{{ lookup('botrus_secret', 'nfs_storage_class') }}"

# =============================================================================
# RUNTIME
# =============================================================================
containerd_cgroup_driver: "{{ lookup('botrus_secret', 'containerd_cgroup_driver') }}"
containerd_log_level: "{{ lookup('botrus_secret', 'containerd_log_level') }}"

# =============================================================================
# SAMBA
# =============================================================================
samba_enabled: "{{ lookup('botrus_secret', 'samba_enabled') }}"
samba_share_name: "{{ lookup('botrus_secret', 'samba_share_name') }}"
samba_share_path: "{{ lookup('botrus_secret', 'samba_share_path') }}"
smb_storage_class: "{{ lookup('botrus_secret', 'smb_storage_class') }}"

# =============================================================================
# SECRET
# =============================================================================
secret_plex_smb-creds_password: "{{ lookup('botrus_secret', 'secret_plex_smb-creds_password') }}"
secret_plex_smb-creds_username: "{{ lookup('botrus_secret', 'secret_plex_smb-creds_username') }}"

# =============================================================================
# STORAGE
# =============================================================================
default_storage_size: "{{ lookup('botrus_secret', 'default_storage_size') }}"
storage_class: "{{ lookup('botrus_secret', 'storage_class') }}"

# =============================================================================
# TRAEFIK
# =============================================================================
traefik_accesslog: "{{ lookup('botrus_secret', 'traefik_accesslog') }}"
traefik_log_level: "{{ lookup('botrus_secret', 'traefik_log_level') }}"
traefik_version: "{{ lookup('botrus_secret', 'traefik_version') }}"

